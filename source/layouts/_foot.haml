
/ Special Handling for Pages that Have Different Modes Available
- if current_page.data.fux_available
  %hr/
  %center
    - if !current_page.url.include?("fux")
      %a.small{:href => "#{current_page.url}fux/"} View as FUX
    - else
      %a.small{:href => "#{current_page.url.gsub('fux/','')}"  } View as Default
  %br/

/
  Bootstrap core JavaScript
  \==================================================
/ Placed at the end of the document so the pages load faster
/ jQuery (necessary for Bootstrap's JavaScript plugins)
%script{src: "https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"}
%script{src: "/javascripts/bootstrap.min.js"}